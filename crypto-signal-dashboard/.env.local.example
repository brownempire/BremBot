# Primary price feed (optional for MVP; app falls back to simulated feed when unset)
NEXT_PUBLIC_CHAOS_EDGE_URL=
NEXT_PUBLIC_CHAOS_EDGE_TOKEN=

# Backup oracle RPCs (optional until Chainlink integration is wired)
NEXT_PUBLIC_ETHEREUM_RPC_URL=
NEXT_PUBLIC_SOLANA_RPC_URL=

# Web push (required for push notifications)
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:you@example.com

# Jupiter Plugin local development toggle
NEXT_PUBLIC_IS_PLUGIN_DEV=true
