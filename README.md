# BremBot
A Solana trading bot. Nothing special.

## Developer workflow
Use `WORKFLOW_DIFFERENT_DEVICE.md` for a copy/paste-safe flow when using Codex from another device.

- Use `scripts/safe-sync-and-push.sh` for a one-command fetch/rebase/build/commit/push flow.
## Auto-trading
See `AUTO_TRADING_IMPLEMENTATION.md` for a practical plan to move from signal-only mode to guarded automatic execution.

